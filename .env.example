# AI Workspace 环境变量（全部可选；不配置时使用内置默认值）

# SQLite 数据库文件路径（默认：data/ai-workspace.db；目录不存在会自动创建）
# 示例：DATABASE_URL=./data/ai-workspace.db
# Docker 容器内固定为：/app/data/ai-workspace.db（见 docker-compose.yml）
# DATABASE_URL=
