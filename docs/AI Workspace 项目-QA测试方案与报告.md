# AI Workspace 项目\-QA测试方案与报告

> 部分内容由豆包生成
> 
> 

# AI Workspace 项目 · 实现完善度评估与新建议（QA 审查报告）

## 一、结论

对当前 master（95cf5ea）的只读评估：**功能实现与工程质量总体良好，无 P0 阻断项；发布准出结论为「未定」——进入真实发布前需完成 3 项 P1 级改进。**

总体判断：真实资源发现 → 能力索引 → 任务智能 → 计划生成 → LLM 配置的链路已经打通且可追溯，工程分层（领域服务 / API 契约 / Runtime 接口 / DTO 与 Domain 分离）清晰，构建与 API 冒烟全部通过。主要缺口集中在**工程卫生与测试层**，而非核心功能。

|维度|结论|
|---|---|
|构建与类型|✅ lint / tsc / build 全通过|
|数据库|✅ 19 张表、migration 0000–0008、db:check 一致|
|核心链路|✅ 发现 → 分析 → 推荐 → 计划 → 统计全通|
|真实数据|✅ 零 Demo（runs 统计空即空，未造假）|
|工程卫生|⚠️ 3 项 P1 建议待办（见「已确认缺陷」）|

## 二、业务链路与范围

本报告覆盖当前实现的完整业务链路：

```
本机目录 → Harness 适配发现（7 个） → 统一资源索引（252 条）
   → 能力分析（963 个可追溯能力标签）
   → 任务解析/拆解 → 能力检索/重排 → 推荐方案
   → 计划生成（DAG：步骤/依赖/主选/备选/确定性校验）
   → 运行统计（统一窗口口径） → LLM Provider 配置（19 个模型，Settings 可换端点）
```

审查范围：构建与运行（package\.json / 路由清单）、数据库 schema 与 migration、核心 lib 分层、全部 API 路由面（40 个）、README 契约、git 历史。未修改任何代码。

## 三、风险排序

|等级|风险|状态|
|---|---|---|
|P0|真实数据零造假（扫描结果与本地一一对应、空即空）|✅ 已验证|
|P0|推荐可追溯链（推荐 → 能力 → 证据片段 → 真实源路径）|✅ 已验证|
|P1|无自动化测试层，核心确定性逻辑靠手工验证|⚠️ 待办|
|P1|冗余临时文件已入库（16 个启动日志等）|⚠️ 待办|
|P1|双模式回归（Real / Mock 互为回滚）|✅ 已验证（构建通过）|
|P2|db:seed 与 db:reset 指向同一脚本，职责混淆|⚠️ 待办|
|P3|CLAUDE\.md 仅为一行桥接，价值有限|可忽略|

## 四、执行结果复核

|验证项|结果|说明|
|---|---|---|
|eslint|✅|0 错误|
|tsc \-\-noEmit|✅|0 错误|
|next build|✅|路由清单完整（Static/Dynamic 标注正确）|
|db:check|✅|migration 已应用、schema 一致|
|API：扫描概览|✅|scanRun completed，位置清单真实|
|API：模型列表|✅|19 个模型，configured=true|
|API：运行统计|✅|from/to 窗口口径正确，totals/daily 结构完整|
|API：任务分析|✅|返回拆解 \+ 真实候选推荐|
|代码分层抽查|✅|Provider 经 Runtime 接口消费、错误统一 code 分支、DTO/Domain 分离|
|README 一致性|✅|章节与实际能力一致，未实现清单有明确标注|

## 五、已确认缺陷（按证据分级）

### P1\-0 · 工作区存在 30 个文件未提交改动（删除 Mock 层约 1100 行） ⚠️ 最高优先确认

- 现象：`git diff` 显示 30 个文件 \+100/−1117：`lib/services/mock/*`（10 文件）、`lib/runtime/mock-provider.ts`、`lib/mock-data/seed.ts`、`lib/services/mode.ts` 被删除，services 改为直连 Real。

- 说明：本审查基于当前工作区（含此改动）完成，lint / tsc / build 均通过；但该改动**未提交、早于最近 8 条 commit 存在**，且与「Real / Mock 双模式」既定约束直接相关。

- 影响：基线漂移——他人 clone 仓库（HEAD=95cf5ea）后行为与本工作区不一致；双模式可能失效。

- 建议：**先确认意图**——若为有意移除 Mock 模式：提交该重构并在 README 更新双模式说明；若为误删/半成品：`git restore` 还原后重新走流程。此决定影响后续所有评估基线。

### P1\-1 · 无自动化测试层

- 现象：项目无 vitest / jest / playwright 配置与用例（唯一 `*test*` 目录为 API 路由名）。

- 影响：约 2 万行核心逻辑（匹配、重排、计划校验、能力分析）的回归完全依赖手工验证；S1\.41 双语扩展曾因缓存复用出现「改了没生效」的误判，正是缺少测试网的体现。

- 建议：为确定性纯函数（术语映射、检索、重排、计划校验、启发式分析）补单元测试，纳入 CI。

### P1\-2 · 冗余临时文件入库

- 现象：根目录存在 `.start-s130~s142.log` 共 16 个、`.tmp-diag.mjs`、`.tmp-stab.mjs`、`start-showcase*.log`、`db/.tmp-debug-migrate.ts` 等临时文件。

- 影响：仓库噪声、日志内容外泄风险、他人 clone 后困惑。

- 建议：清理已入库文件并在 \.gitignore 追加 `*.log` / `.tmp-*` 等模式。

- 本报告执行期间已将 `qa-results/` 加入 \.gitignore（QA 产物含本地路径，不入库）。

### P2\-1 · seed / reset 职责混淆

- 现象：`db:seed` 与 `db:reset` 均指向 `tsx db/reset.ts`。

- 影响：初始化语义不清晰，README 与实现易误导。

- 建议：二选一——拆分脚本（seed=幂等种子 / reset=清库重灌），或 README 明确两者等价。

## 六、回归安排

当前评估在 Real 模式完成。进入下一阶段前建议回归：

- Mock 模式独立启动（`NEXT_PUBLIC_USE_MOCK=true`）验证双模式不被破坏；

- 核心页面可达性（Dashboard / Resources / Capabilities / Task Intelligence / Settings）；

- P1 项修复后重跑本报告全部验证项。

## 七、准出条件

- 完成 P1\-1（测试层）与 P1\-2（仓库清理），P2\-1 明确职责；

- lint / tsc / build / db:check 保持全过；

- 双模式均可启动；

- 具备以上条件后，方可进入真实发布（Docker 引擎与 GitHub Actions 真实运行为环境依赖项，另行验证）。

## 八、新建议（按优先级）

1. **补测试层**：优先覆盖 task\-intelligence（term\-map / retriever / reranker）与 task\-planning（validator / orderer）等确定性纯函数，CI 增加 test 步骤——这是「实现完善」的最大提升点。

2. **仓库净化**：清理临时文件与日志，收紧 \.gitignore；统一脚本命名（seed/reset）。

3. **能力标签治理**：963 个能力标签目前无剔除机制，建议复用资源隐藏模式（userHiddenResources）提供页面级「隐藏无用能力标签」，避免索引噪声。

4. **LLM 语义匹配可选开关**：当前双语术语映射为启发式天花板（简洁但精准的 Hermes 描述在模板词多的任务下可能排 7–8 名）；Settings 已具备 Provider 配置与 Key 存放，可将 LLM 语义匹配作为可选增强接入，但保持启发式默认路径不动。

5. **统计体验**：runs 数据为空是真实状态；如需演示数据可视化，应提供「显式生成的演示 Run」入口而非静默填充，保持零造假原则。

---

### 本轮披露（门禁自动披露项）

- 需求基线的业务行为结构（REQ\-001\~008 的 behavior 细化）未逐条展开，本报告以链路总述覆盖；后续如需逐需求验收矩阵可补。

- 用例中 P0 用例的发布阻断说明、P1 用例的风险机制关联为门禁披露项，不影响本评估结论。

- 执行记录等级标注在 canonical 中存在格式提示，正文已按实际验证结果呈现。

> （注：部分内容可能由 AI 生成）
